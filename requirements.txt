# Python 3.10+ 권장 (서버 3.12.3 등)
# 설치: pip install -r requirements.txt
# Playwright 브라우저: playwright install chromium

# --- API (main.py) ---
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pydantic>=2.0.0

# --- KoBERT 엔진 (TEST_27_server.py 등) ---
torch>=2.0.0
transformers>=4.30.0
sentencepiece>=0.1.99

# --- HTTP / HTML / 브라우저 ---
requests>=2.28.0
beautifulsoup4>=4.12.0
curl-cffi>=0.6.0
playwright>=1.40.0

# --- XGBoost URL 모델 (XG_core.py, *.joblib) ---
joblib>=1.3.0
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
scikit-learn>=1.6.0
xgboost>=2.0.0

# --- OPQR / RF (gnn_infer.py, opqr_model.pkl) ---
# pandas·scikit-learn·joblib 위와 공유

# --- 스크립트·실험 (gnn-ready.py: RF 학습·시각화 / PyG 예제) ---
matplotlib>=3.7.0
seaborn>=0.12.0
networkx>=3.0
torch-geometric>=2.4.0
